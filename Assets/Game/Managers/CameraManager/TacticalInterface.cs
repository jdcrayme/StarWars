using System;
using UnityEngine;

namespace Assets.Game.Managers.CameraManager
{
/*	public class TacticalInterface : MonoBehaviour, CameraManager.ICamera
	{
	    private Camera _camera;

		public Camera Camera { get { return _camera; } }

        public bool ShowWaypoints { get { return true; } }

	    // Use this for initialization
		void Start () {
		}
	
		// Update is called once per frame
		void Update () {
	
		}

		public void Activate(GameObject target)
		{
            _camera = (Camera)gameObject.GetComponentInChildren(typeof(Camera));
		}

		public void Deactivate()
		{
		}
	}*/
}
