using Assets.Game.Managers.CameraManager;
using Assets.Game.Ships.Scripts;
using UnityEngine;
using System.Collections;

public class InputManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {

/*        if (Input.GetKeyDown("f1"))
            CameraManager.Instance.SetCamera(CameraManager.CameraMode.Tactical);

        if (Input.GetKeyDown("f2"))
            CameraManager.Instance.SetCamera(CameraManager.CameraMode.Pilot);

        if (Input.GetKeyDown("f3"))
            CameraManager.Instance.SetCamera(CameraManager.CameraMode.Follow);
        
        if (Input.GetKeyDown("f5"))
            CameraManager.Instance.SetTarget(SpaceUnit.GetNextUnit().gameObject);

        if (Input.GetKeyDown("f8"))
        {
        }*/

	}
}
